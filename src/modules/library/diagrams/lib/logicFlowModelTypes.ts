import type { BaseNodeModel, GraphModel } from '@logicflow/core'

/** LogicFlow 节点 / 图模型类型（勿用 LogicFlow.BaseNodeModel 命名空间，d.ts 未导出） */
export type { BaseNodeModel, GraphModel }

export type LfNodeModel = BaseNodeModel
export type LfGraphModel = GraphModel
