---
alwaysApply: true
---

# 通用规则（AI 请尽量遵守）

## 工具使用
- 优先使用 `/context7-mcp` 获取最新 API 或技术方案时。
- 使用 `sequential-thinking` MCP 进行复杂逻辑或多步骤决策。
- 使用 `supermemory` MCP 存储关键信息，保持上下文连续性。
- 组合使用 `fetch` 和 `exa` MCP 获取网页资料。
- 需要搜索 UI 组件或特效动画时，使用 `magic` MCP。

## 文档与代码
- 需求、设计、计划文档请放在 `./doc/` 目录，使用 Markdown 格式，命名参考已有文件。
- 编码尽量遵循 SOLID 原则、模块化设计，优先复用已有组件和逻辑。
- UI 风格宜简约高级，适配深色/浅色模式。CSS 优先写在 Vue 组件的 `<style scoped>` 内，全局通用样式才放入 `src/app/styles`。
- UI 组件选择优先级：`src/shared/components` → 三方库 → 二次封装 → 完全自定义。
- 临时脚本或文档使用后请及时删除；目录迁移后请删除空目录。
- Git 操作失败时，可尝试设置临时代理后重试。

# 软件设计规则（重点）

## 目录结构
大模块按照现有目录已经组织好，可以不用调整（如需调整请告知我来决策），子模块源码结构建议按以下逻辑分层（如果没有对应分层可以不需要）：

- `domain/`：业务实体、值对象、领域服务、仓储接口
- `app/`：用例、应用服务、DTO、命令/查询处理
- `services/`：基础设施（数据库、外部 API、文件、日志、缓存）
- `interfaces/`：通用接口、对外接口、路由、控制器、事件监听等
- `shared/`：通用组件、工具、常量、配置
- `views/`：页面级 UI
- `components/`：模块内 UI 组件（全局通用组件请放 `shared/components`）
- 其他合理的分层可适当添加

文件命名尽量清晰，避免非通用缩写。每个目录可包含 `index.ts` 简化导出。

## 设计原则
- **开闭原则**：优先定义抽象接口，默认实现可替换，尽量通过扩展而非修改原代码来增加功能。
- **依赖倒置**：高层模块不应直接依赖低层模块，两者宜依赖抽象；推荐使用依赖注入来解耦。
- **单一职责**：每个类或函数最好只有一个明确的变化理由，避免万能类。
- **接口隔离**：接口设计应小而专，避免包含实现者不需要的方法。
- **里氏替换**：子类或实现类应能完全替代父类或抽象，不改变预期行为。

## 扩展性机制
- 对于可能变化的功能（如存储方式、第三方服务、算法、认证），建议先定义抽象接口（可加 `I` 前缀），再提供默认实现（加技术后缀，如 `SqlUserRepository`）。
- 切换实现时，尽量通过配置文件或依赖注入容器调整绑定，避免在业务代码中直接 `new` 具体类。

## 存储设计
- 推荐使用仓储模式统一数据访问接口，以便支持本地存储和云端存储切换。
- 本地存储最好按模块分文件；数据量大时可用数据库并按业务分表。

## 配置管理
- 配置、常量、注册项需要统一管理。
- 功能变更尽量只通过修改配置实现，避免硬编码可能变化的值。

## 依赖注入
- 建议在应用入口（组合根）统一注册接口与实现。
- 尽量通过构造函数注入依赖，减少在业务类内部直接创建依赖对象。

## AI 行为参考
当生成代码时，AI 可以考虑以下流程：
1. 识别需求中潜在的扩展点。
2. 为扩展点定义抽象接口。
3. 提供默认实现。
4. 在组合根中注册依赖绑定。
5. 输出符合目录结构的文件，按需提供 `index.ts`。
6. 发现重复代码时，可提取到 `shared/` 目录。