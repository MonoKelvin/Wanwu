---
alwaysApply: true
---

# 通用规则（AI 请务必遵守）

## 工具使用
- 优先使用 `/context7-mcp` 获取最新 API 或技术方案时。
- 可以使用 `sequential-thinking` MCP 进行复杂逻辑或多步骤决策。
- 可以使用 `supermemory` MCP 存储关键信息，保持上下文连续性。
- 组合使用 `fetch` 和 `exa` MCP 获取网页资料。
- 需要搜索 UI 组件或特效动画时，使用 `magic` MCP。
- Git 操作失败时，可尝试设置临时代理后重试。

## 文档与代码
- 需求、设计、计划文档请放在 `./doc/` 目录，使用 Markdown 格式，命名参考已有文件。
- 编码尽量遵循 SOLID 原则、模块化设计，优先复用已有组件和逻辑。
- UI 风格宜简约高级，适配深色/浅色模式。CSS 优先写在 Vue 组件的 `<style scoped>` 内，全局通用样式才放入 `src/app/styles`。
- UI 组件选择优先级：`src/shared/components` → 三方库 → 二次封装 → 完全自定义。
- 临时脚本或文档使用后请及时删除；目录迁移、删除后请删除空目录。

# 软件设计规则（重点）

## 目录结构
大模块按照现有目录已经组织好，可以不调整（如需调整请告知我来决策），子模块源码结构建议按层级功能划分，但是要求**使用最少的目录、文件**实现。文件命名尽量清晰简短易读。设计模块的时候尽量按照分层设计，可能会变化的功能都应该抽象出接口、稳定的内容设计成框架机制，尽量复用通用框架外的机制和工具。

## 设计原则
- **开闭原则**：优先定义抽象接口，默认实现可替换，尽量通过扩展而非修改原代码来增加功能。软件通用机制、框架（electron、src/app、src/shared目录）不能写任何业务逻辑，如果需要写则抽象接口、服务、注册扩展点方式做成通用机制。
- **解耦和模块化**: 各个子模块应该最大程度设计成独立的模块，模块之间要解耦，只能通过接口、服务等方式通讯。

## 扩展性机制
- 对于可能变化的功能（如存储方式、第三方服务、算法、认证），建议先定义抽象接口（可加 `I` 前缀），再提供默认实现（加技术后缀，如 `SqlUserRepository`）。

## 存储设计
- 推荐使用仓储模式统一数据访问接口，以便支持本地存储和云端存储切换。
- 本地存储最好按模块分文件；数据量大时可用数据库并按业务分表。

## 配置管理
- 配置、常量、注册项需要统一管理。
- 功能变更尽量只通过修改配置实现，避免硬编码可能变化的值。

## AI 行为参考
当生成代码时，AI 可以考虑以下流程：
1. 识别需求中潜在的扩展点。
2. 为扩展点定义抽象接口。
3. 提供默认实现。
4. 在组合根中注册依赖绑定。
5. 输出符合目录结构的文件，按需提供 `index.ts`。
6. 发现重复的非业务代码时，可提取到 `shared/` 目录。
7. 当发现不确定的需求时请提前询问我了解具体情况再开展文档、代码的编写